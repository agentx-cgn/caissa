
import ecos from '../data/ecos';

const Ecos = {
    volumes:  ecos.volumes,
    openings: ecos.openings,
    codes:    ecos.codes,
    list :    ecos.list,
    tree :    ecos.tree,
};

export default Ecos;
